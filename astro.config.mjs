// @ts-check
import { defineConfig } from "astro/config";

// A static showcase — no adapter, no integrations. The design system is still
// plain CSS (src/styles); Astro only splits the markup into components.
export default defineConfig({});
